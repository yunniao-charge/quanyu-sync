---
name: qa
description: 交互式QA会话，用户以对话方式报告bug或问题，代理提交GitHub Issues。在后台探索代码库以获取上下文和领域语言。当用户想要报告bug、做QA、以对话方式提交Issues，或提到"QA session"时使用。
---

# QA会话

运行交互式QA会话。用户描述他们遇到的问题。你进行澄清，在后台探索代码库获取上下文，并提交持久的、面向用户的、使用项目领域语言的GitHub Issues。

## 对于用户提出的每个问题

### 1. 倾听并轻度澄清

让用户用自己的话描述问题。**最多问2-3个简短的澄清问题**，聚焦于：

- 他们期望发生什么 vs 实际发生了什么
- 重现步骤（如果不明显）
- 是持续发生还是间歇性发生

不要过度访谈。如果描述足够清晰可以提交，就继续。

### 2. 在后台探索代码库

在与用户交谈时，在后台启动Agent（subagent_type=Explore）了解相关领域。目标不是找修复方案——而是：

- 学习该领域使用的领域语言（检查UBIQUITOUS_LANGUAGE.md）
- 理解功能应该做什么
- 识别面向用户的行为边界

这个上下文帮助你写出更好的Issue——但Issue本身不应引用特定文件、行号或内部实现细节。

### 3. 评估范围：单一Issue还是拆分？

在提交前，决定这是一个**单一Issue**还是需要**拆分**为多个Issue。

需要拆分的情况：

- 修复跨越多个独立领域（如"表单验证错误并且成功消息缺失并且重定向损坏"）
- 有明显可分离的关注点，不同的人可以并行工作
- 用户描述的事物有多种不同的故障模式或症状

保持单一Issue的情况：

- 是一个地方的一种行为错误
- 所有症状都由同一个根本行为引起

### 4. 提交GitHub Issue(s)

使用`gh issue create`创建Issues。不要让用户先审阅——直接提交并分享URL。

Issue必须**持久**——即使经过重大重构仍有意义。从用户角度编写。

#### 对于单一Issue

使用此模板：

```
## 发生了什么

[用通俗语言描述用户实际体验到的行为]

## 我期望什么

[描述预期行为]

## 重现步骤

1. [开发者可以遵循的具体编号步骤]
2. [使用代码库中的领域术语，而非内部模块名]
3. [包含相关的输入、标志或配置]

## 额外上下文

[来自用户或代码库探索的任何额外观察，帮助框定问题——如"这只在使用Docker层时发生，而非文件系统层"——使用领域语言但不要引用文件]
```

#### 对于拆分（多个Issues）

按依赖顺序创建Issues（阻塞者优先），以便引用真实的Issue编号。

对每个子Issue使用此模板：

```
## 父Issue

#<父Issue编号>（如果创建了跟踪Issue）或"QA会话期间报告"

## 问题

[描述此特定行为问题——仅此切片，不是整个报告]

## 我期望什么

[此特定切片的预期行为]

## 重现步骤

1. [此Issue特定的步骤]

## 被阻塞

- #<Issue编号>（如果此Issue必须等另一个解决后才能修复）

或"无 - 可立即开始"如果无阻塞。

## 额外上下文

[与此切片相关的任何额外观察]
```

创建拆分时：

- **宁可多个薄Issue也不要少数厚Issue**——每个都应该可以独立修复和验证
- **诚实标记阻塞关系**——如果Issue B真的要等Issue A修复后才能测试，就说明。如果它们独立，都标记为"无 - 可立即开始"
- **按依赖顺序创建Issues**以便在"被阻塞"中引用真实的Issue编号
- **最大化并行度**——目标是多个人（或代理）可以同时领取不同的Issues

#### 所有Issue正文的规则

- **不要文件路径或行号**——这些会过时
- **使用项目的领域语言**（如果存在，检查UBIQUITOUS_LANGUAGE.md）
- **描述行为，而非代码**——"同步服务未能应用补丁"而非"applyPatch()在第42行抛出异常"
- **重现步骤是必须的**——如果无法确定，询问用户
- **保持简洁**——开发者应该能在30秒内读完Issue

提交后，打印所有Issue URL（总结阻塞关系）并询问："下一个问题，还是结束了？"

### 5. 继续会话

继续直到用户说结束。每个Issue独立处理——不要批量处理。

---

## 总结与设计思路讲解

### 主要功能

`qa` 是一个**交互式问题收集与Issue创建技能**，通过自然对话收集用户反馈并转化为结构化的GitHub Issues。

### 设计思路

1. **对话优先**：模仿真实QA会话的体验——让用户自然地描述问题，而非填表式问答。这降低了用户参与的门槛。

2. **后台探索**：在对话进行时并行探索代码库，获取领域语言和上下文。这样Issue可以使用项目的真实术语，而非用户可能不准确的表达。

3. **Issue持久性原则**：强调Issue应该描述行为而非实现细节。即使代码库重构，描述"用户无法登录"的Issue仍然有效，而"LoginService.authenticate()第42行错误"会很快过时。

4. **拆分决策**：自动识别问题是否需要拆分为多个Issues。这避免了大而模糊的Issue，提高了可执行性。

5. **并行度优化**：通过标记阻塞关系，让团队知道哪些Issues可以并行工作，哪些有依赖。

6. **不预审**：直接创建Issue而不让用户先审阅。这加快了流程，且Issue可以后续编辑。

### 适用场景

- 产品发布后的bug收集
- 用户反馈的整理与追踪
- 敏捷回顾会议的问题记录
- 技术支持的Issue创建
- 跨团队问题的标准化记录
