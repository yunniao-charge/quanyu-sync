---
name: tdd
description: 红绿重构循环的测试驱动开发。当用户想要使用TDD构建功能或修复bug、提到"red-green-refactor"、想要集成测试，或请求测试优先开发时使用。
---

# 测试驱动开发

## 哲学

**核心原则**：测试应该通过公共接口验证行为，而非实现细节。代码可以完全改变；测试不应改变。

**好的测试**是集成风格的：它们通过公共API执行真实的代码路径。它们描述系统做什么，而非如何做。好的测试读起来像规格说明——"用户可以用有效购物车结账"准确告诉你存在什么能力。这些测试能在重构中存活，因为它们不关心内部结构。

**坏的测试**与实现耦合。它们模拟内部协作者、测试私有方法，或通过外部手段验证（如直接查询数据库而非使用接口）。警告信号：你重构时测试失败，但行为没变。如果你重命名一个内部函数而测试失败，那些测试测试的是实现，不是行为。

参见[tests.md](tests.md)获取示例，[mocking.md](mocking.md)获取模拟指南。

## 反模式：水平切片

**不要先写所有测试，然后写所有实现。** 这是"水平切片"——把RED当作"写所有测试"，GREEN当作"写所有代码"。

这会产生**垃圾测试**：

- 批量编写的测试测试的是想象的测试，而非实际行为
- 你最终测试的是事物的形态（数据结构、函数签名）而非面向用户的行为
- 测试对真实变化变得不敏感——行为损坏时它们通过，行为正常时它们失败
- 你超越了你的车灯，在理解实现之前就承诺了测试结构

**正确方法**：通过追踪弹实现垂直切片。一个测试 → 一个实现 → 重复。每个测试回应你从前一个循环中学到的东西。因为你刚写了代码，你确切知道什么行为重要以及如何验证它。

```
错误（水平）：
  RED:   test1, test2, test3, test4, test5
  GREEN: impl1, impl2, impl3, impl4, impl5

正确（垂直）：
  RED→GREEN: test1→impl1
  RED→GREEN: test2→impl2
  RED→GREEN: test3→impl3
  ...
```

## 工作流程

### 1. 规划

在编写任何代码之前：

- [ ] 与用户确认需要什么接口变更
- [ ] 与用户确认要测试哪些行为（优先排序）
- [ ] 识别[深模块](deep-modules.md)的机会（小接口、深实现）
- [ ] 为[可测试性](interface-design.md)设计接口
- [ ] 列出要测试的行为（非实现步骤）
- [ ] 获得用户对计划的批准

询问："公共接口应该是什么样的？哪些行为最重要需要测试？"

**你不能测试一切。** 与用户确认确切哪些行为最重要。将测试精力集中在关键路径和复杂逻辑上，而非每个可能的边缘情况。

### 2. 追踪弹

编写一个测试，确认系统的一件事：

```
RED:   为第一个行为编写测试 → 测试失败
GREEN: 编写最小代码使其通过 → 测试通过
```

这是你的追踪弹——证明端到端路径可行。

### 3. 增量循环

对于每个剩余行为：

```
RED:   编写下一个测试 → 失败
GREEN: 使其通过的最小代码 → 通过
```

规则：

- 一次一个测试
- 只编写足够的代码通过当前测试
- 不要预测未来的测试
- 保持测试聚焦于可观察的行为

### 4. 重构

所有测试通过后，寻找[重构候选](refactoring.md)：

- [ ] 提取重复
- [ ] 深化模块（将复杂性移到简单接口后面）
- [ ] 在自然的地方应用SOLID原则
- [ ] 考虑新代码揭示了现有代码的什么
- [ ] 每个重构步骤后运行测试

**永远不要在RED时重构。** 先回到GREEN。

## 每循环检查清单

```
[ ] 测试描述行为，而非实现
[ ] 测试只使用公共接口
[ ] 测试能在内部重构中存活
[ ] 代码对当前测试是最小的
[ ] 没有添加投机性功能
```

---

## 总结与设计思路讲解

### 主要功能

`tdd` 是一个**测试驱动开发技能**，提供完整的TDD工作流程指南和最佳实践。

### 设计思路

1. **行为驱动测试**：核心哲学是测试应该验证"做什么"而非"如何做"。这使得测试成为系统的活文档，而非与实现紧耦合的脆弱检查。

2. **垂直切片 vs 水平切片**：这是本技能最重要的洞察：
   - 水平切片：先写所有测试再写所有实现——被明确标记为反模式
   - 垂直切片：一个测试→一个实现→重复——正确方法

   垂直切片的好处是每个测试都基于刚写的代码的真实理解，而非想象中的行为。

3. **追踪弹概念**：源自软件工程，指第一条贯穿所有层的端到端路径。它证明架构可行，也为后续开发提供参考。

4. **深模块理念**：强调小接口隐藏大实现的模块设计，这是可测试性和可维护性的基础。

5. **增量主义**：一次一个测试、最小实现、不预测未来。这避免了过度工程和YAGNI（You Aren't Gonna Need It）问题。

6. **重构安全网**：强调"不要在RED时重构"——先让测试通过，再安全地重构。

### 适用场景

- 新功能开发
- Bug修复的测试先行
- 重构时的测试保护
- 团队TDD实践培训
- 测试策略制定
