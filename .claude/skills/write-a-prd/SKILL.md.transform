---
name: write-a-prd
description: 通过用户访谈、代码库探索和模块设计创建PRD，然后提交为GitHub Issue。当用户想要编写PRD、创建产品需求文档或规划新功能时使用。
---

此技能在用户想要创建PRD时调用。如果你认为某些步骤不必要，可以跳过。

1. 询问用户关于他们想要解决的问题以及任何潜在解决方案想法的长篇详细描述。

2. 探索代码库以验证他们的断言并了解代码库的当前状态。

3. 对计划的每个方面进行无情访谈，直到达成共识。沿着决策树的每个分支逐一解决决策之间的依赖关系。

4. 草拟完成实现需要构建或修改的主要模块。积极寻找提取可独立测试的深模块的机会。

深模块（与浅模块相对）是一个将大量功能封装在简单、可测试且很少变化的接口中的模块。

与用户确认这些模块符合他们的期望。与用户确认他们想要为哪些模块编写测试。

5. 一旦你对问题和解决方案有了完整理解，使用下面的模板编写PRD。PRD应作为GitHub Issue提交。

<PRD模板>

## 问题陈述

用户面临的问题，从用户的视角描述。

## 解决方案

问题的解决方案，从用户的视角描述。

## 用户故事

一份长篇、编号的用户故事列表。每个用户故事应采用以下格式：

1. 作为<角色>，我想要<功能>，以便<收益>

<用户故事示例>
1. 作为手机银行客户，我想要查看账户余额，以便我能更好地了解支出情况做出决策
</用户故事示例>

此用户故事列表应极其详尽，覆盖功能的所有方面。

## 实现决策

做出的实现决策列表。可以包括：

- 将要构建/修改的模块
- 这些模块将要修改的接口
- 来自开发者的技术澄清
- 架构决策
- 模式变更
- API契约
- 具体交互

不要包含特定文件路径或代码片段。它们可能很快就会过时。

## 测试决策

做出的测试决策列表。包括：

- 什么是好测试的描述（只测试外部行为，不测试实现细节）
- 哪些模块将被测试
- 测试的先例（即代码库中类似的测试类型）

## 范围外

此PRD范围外的事项描述。

## 其他说明

关于功能的任何其他说明。

</PRD模板>

---

## 总结与设计思路讲解

### 主要功能

`write-a-prd` 是一个**产品需求文档编写技能**，通过深度访谈和代码探索帮助用户创建全面的PRD。

### 设计思路

1. **深度访谈优先**：使用"grill-me"式的访谈方法，沿着决策树逐一解决问题。这确保PRD基于充分的思考，而非表面的想法。

2. **代码库验证**：不盲目接受用户描述，而是去代码库验证其断言。这发现问题用户可能忽略的约束和现有模式。

3. **模块设计意识**：在PRD阶段就思考模块边界，特别是寻找"深模块"机会——小接口封装大功能的模块。这为后续的可测试架构奠定基础。

4. **用户故事驱动**：使用标准格式"作为<角色>，我想要<功能>，以便<收益>"列出详尽的用户故事。这是连接业务需求和技术实现的桥梁。

5. **持久性设计**：
   - 不包含文件路径和代码片段
   - 描述模块、接口、契约而非具体实现
   - 这样PRD在代码演进后仍有价值

6. **范围明确**：明确列出"范围外"事项，防止需求蔓延。

7. **测试考量**：在PRD阶段就考虑测试策略，确保实现是可测试的。

### 适用场景

- 新功能规划
- 产品需求文档编写
- 技术方案立项
- 跨团队项目沟通
- 产品经理与开发团队协作
