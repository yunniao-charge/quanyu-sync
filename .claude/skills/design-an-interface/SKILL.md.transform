---
name: design-an-interface
description: 使用并行子代理为模块生成多个截然不同的接口设计。当用户想要设计API、探索接口选项、比较模块形态，或提到"design it twice"时使用。
---

# 设计接口

基于《软件设计哲学》中的"设计两次"原则：你的第一个想法不太可能是最好的。生成多个截然不同的设计，然后进行比较。

## 工作流程

### 1. 收集需求

在设计之前，需要了解：

- [ ] 这个模块解决什么问题？
- [ ] 调用者是谁？（其他模块、外部用户、测试）
- [ ] 关键操作有哪些？
- [ ] 有什么约束？（性能、兼容性、现有模式）
- [ ] 哪些应该隐藏在内部，哪些应该暴露？

询问："这个模块需要做什么？谁会使用它？"

### 2. 生成设计（并行子代理）

使用Task工具同时启动3个以上的子代理。每个代理必须产生一个**截然不同**的方案。

```
每个子代理的提示词模板：

为以下模块设计接口：[模块描述]

需求：[收集的需求]

此设计的约束：[为每个代理分配不同的约束]
- 代理1："最小化方法数量 - 最多1-3个方法"
- 代理2："最大化灵活性 - 支持多种用例"
- 代理3："为最常见的情况优化"
- 代理4："从[特定范式/库]获取灵感"

输出格式：
1. 接口签名（类型/方法）
2. 使用示例（调用者如何使用它）
3. 此设计在内部隐藏了什么
4. 这种方法的权衡
```

### 3. 展示设计

展示每个设计时包括：

1. **接口签名** - 类型、方法、参数
2. **使用示例** - 调用者在实践中如何使用它
3. **隐藏的内容** - 保留在内部的复杂性

按顺序展示设计，让用户在比较之前可以充分理解每个方案。

### 4. 比较设计

展示所有设计后，从以下维度进行比较：

- **接口简洁性**：方法更少、参数更简单 = 更容易学习和正确使用
- **通用 vs 专用**：灵活性 vs 专注度
- **实现效率**：接口形态是否允许高效的内部实现？
- **深度**：小接口隐藏大量复杂性（好）vs 大接口配薄实现（坏）
- **正确使用的容易度** vs **误用的容易度**

用文字讨论权衡，不要用表格。突出设计分歧最大的地方。

### 5. 综合

通常最好的设计会结合多个方案的见解。询问：

- "哪个设计最符合你的主要用例？"
- "其他设计中有值得融入的元素吗？"

## 评估标准

来自《软件设计哲学》：

**接口简洁性**：方法更少、参数更简单 = 更容易学习和正确使用。

**通用性**：无需更改即可处理未来的用例。但要警惕过度泛化。

**实现效率**：接口形态是否允许高效的实现？还是会迫使内部实现变得别扭？

**深度**：小接口隐藏大量复杂性 = 深模块（好）。大接口配薄实现 = 浅模块（避免）。

## 反模式

- 不要让子代理产生相似的设计 - 强制要求根本性差异
- 不要跳过比较 - 价值在于对比
- 不要实现 - 这纯粹是关于接口形态
- 不要基于实现工作量来评估

---

## 总结与设计思路讲解

### 主要功能

`design-an-interface` 是一个**接口设计辅助技能**，核心理念是"设计两次"——避免陷入第一个设计方案，通过生成多个截然不同的设计来发现最佳方案。

### 设计思路

1. **并行探索**：使用多个子代理同时工作，每个代理被赋予不同的设计约束（如最小化方法数、最大化灵活性等），确保产出的设计方案具有根本性差异。

2. **结构化对比**：不是简单列出方案，而是从接口简洁性、通用性、实现效率、模块深度等多个维度进行深度对比。

3. **深度模块理念**：源自John Ousterhout的《软件设计哲学》，强调好的接口应该是"小接口隐藏大实现"——接口简单但功能强大。

4. **避免过早实现**：此技能严格限制在设计阶段，不涉及具体实现，让设计者专注于接口形态而非实现细节。

### 适用场景

- 设计新模块的API
- 重构现有接口
- 需要在多种设计方向间做决策
- 团队讨论接口设计方案
