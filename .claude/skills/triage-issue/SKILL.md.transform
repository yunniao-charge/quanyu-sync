---
name: triage-issue
description: 通过探索代码库找到根本原因来分类bug或问题，然后创建带有TDD修复计划的GitHub Issue。当用户报告bug、想要提交Issue、提到"triage"，或想要调查和规划问题修复时使用。
---

# 问题分类

调查报告的问题，找到其根本原因，并创建带有TDD修复计划的GitHub Issue。这是一个几乎不需要人工干预的工作流程——最小化向用户提问。

## 流程

### 1. 捕获问题

从用户获取问题的简要描述。如果他们没有提供，问一个问题："你看到了什么问题？"

不要问后续问题。立即开始调查。

### 2. 探索和诊断

使用Agent工具（subagent_type=Explore）深入调查代码库。你的目标是找到：

- **哪里**bug表现（入口点、UI、API响应）
- **什么**代码路径涉及（追踪流程）
- **为什么**它失败（根本原因，不只是症状）
- **存在什么**相关代码（类似模式、测试、相邻模块）

查看：
- 相关源文件及其依赖
- 现有测试（测试了什么、缺少什么）
- 受影响文件的最近更改（相关文件上的`git log`）
- 代码路径中的错误处理
- 代码库中其他地方正确工作的类似模式

### 3. 确定修复方法

基于调查，确定：

- 修复根本原因需要的最小变更
- 涉及哪些模块/接口
- 需要通过测试验证哪些行为
- 这是回归、缺失功能还是设计缺陷

### 4. 设计TDD修复计划

创建一个具体的、有序的RED-GREEN循环列表。每个循环是一个垂直切片：

- **RED**：描述捕获损坏/缺失行为的特定测试
- **GREEN**：描述使该测试通过的最小代码变更

规则：
- 测试通过公共接口验证行为，而非实现细节
- 一次一个测试，垂直切片（不是所有测试先写，然后所有代码）
- 每个测试应该能在内部重构中存活
- 如果需要包含最终重构步骤
- **持久性**：只建议能在激进代码库变更中存活的修复。描述行为和契约，而非内部结构。测试断言可观察的结果（API响应、UI状态、用户可见效果），而非内部状态。好的建议读起来像规格说明；坏的读起来像diff。

### 5. 创建GitHub Issue

使用`gh issue create`创建GitHub Issue，使用下面的模板。不要在创建前让用户审阅——直接创建并分享URL。

<Issue模板>

## 问题

bug或问题的清晰描述，包括：
- 发生了什么（实际行为）
- 应该发生什么（预期行为）
- 如何重现（如适用）

## 根本原因分析

描述你在调查中发现的内容：
- 涉及的代码路径
- 为什么当前代码失败
- 任何促成因素

不要包含特定文件路径、行号或与当前代码布局耦合的实现细节。改为描述模块、行为和契约。Issue即使在重大重构后仍应有价值。

## TDD修复计划

RED-GREEN循环的编号列表：

1. **RED**：编写一个[描述预期行为]的测试
   **GREEN**：[使其通过的最小变更]

2. **RED**：编写一个[描述下一个行为]的测试
   **GREEN**：[使其通过的最小变更]

...

**REFACTOR**：[所有测试通过后需要的任何清理]

## 验收标准

- [ ] 标准1
- [ ] 标准2
- [ ] 所有新测试通过
- [ ] 现有测试仍然通过

</Issue模板>

创建Issue后，打印Issue URL和根本原因的一行摘要。

---

## 总结与设计思路讲解

### 主要功能

`triage-issue` 是一个**问题诊断与修复规划技能**，帮助深入分析bug并生成结构化的修复计划。

### 设计思路

1. **最小打扰原则**：只问用户一个问题（"你看到了什么问题？"），然后自主调查。这尊重用户时间，也体现了AI代理的自主能力。

2. **深入探索**：使用探索型子代理进行多维度调查：
   - 问题表现位置（入口点）
   - 代码路径追踪
   - 根本原因挖掘
   - 相关代码发现

   这确保了修复方案是基于证据而非猜测。

3. **TDD修复计划**：不是简单说"修好这个bug"，而是提供可执行的RED-GREEN循环列表。每个循环是一个垂直切片，保证了增量、安全的修复方式。

4. **持久性原则**：强调Issue不应包含会过时的细节（文件路径、行号）。这样即使代码库重构，Issue仍提供价值。Issue应该读起来像规格说明。

5. **根本原因导向**：明确区分"症状"和"根本原因"，确保修复针对根源而非表面问题。

6. **自动创建Issue**：不需要用户审阅就创建，加快流程。Issue可以后续编辑。

### 适用场景

- 生产环境bug调查
- 用户报告问题的深入分析
- 复杂问题的根因分析
- 团队问题交接
- 技术债务的修复规划
