---
name: request-refactor-plan
description: 通过用户访谈创建详细的重构计划，包含小步骤提交，然后提交为GitHub Issue。当用户想要规划重构、创建重构RFC，或将重构拆分为安全的增量步骤时使用。
---

此技能在用户想要创建重构请求时调用。你应该按照以下步骤进行。如果你认为某些步骤不必要，可以跳过。

1. 询问用户关于他们想要解决的问题以及任何潜在解决方案想法的长篇详细描述。

2. 探索代码库以验证他们的断言并了解代码库的当前状态。

3. 询问他们是否考虑过其他选项，并向他们展示其他选项。

4. 对实现进行访谈。要极其详细和彻底。

5. 确定实现的确切范围。确定你计划更改什么和不更改什么。

6. 查看代码库以检查此区域的测试覆盖率。如果测试覆盖率不足，询问用户他们的测试计划是什么。

7. 将实现拆分为小步骤提交计划。记住Martin Fowler的建议："让每个重构步骤尽可能小，这样你总能看到程序在工作。"

8. 使用GitHub Issue创建重构计划。使用以下模板作为Issue描述：

<重构计划模板>

## 问题陈述

开发者面临的问题，从开发者的视角描述。

## 解决方案

问题的解决方案，从开发者的视角描述。

## 提交计划

一份长篇详细的实现计划。用通俗英语编写计划，将实现拆分为尽可能小的提交。每个提交应该让代码库保持在可工作状态。

## 决策文档

做出的实现决策列表。可以包括：

- 将要构建/修改的模块
- 这些模块将要修改的接口
- 来自开发者的技术澄清
- 架构决策
- 模式变更
- API契约
- 具体交互

不要包含特定文件路径或代码片段。它们可能很快就会过时。

## 测试决策

做出的测试决策列表。包括：

- 什么是好测试的描述（只测试外部行为，不测试实现细节）
- 哪些模块将被测试
- 测试的先例（即代码库中类似的测试类型）

## 范围外

重构范围外的事项描述。

## 其他说明（可选）

关于重构的任何其他说明。

</重构计划模板>

---

## 总结与设计思路讲解

### 主要功能

`request-refactor-plan` 是一个**重构计划制定技能**，通过深度访谈帮助用户将重构想法转化为详细的、可执行的计划。

### 设计思路

1. **深度访谈优先**：在动手规划前，通过访谈彻底理解问题。这避免了"解决错误问题"的风险。

2. **代码库验证**：不盲目相信用户的描述，而是去代码库验证。这发现用户可能遗漏的细节或错误假设。

3. **方案对比**：主动提出其他选项，防止用户过早锁定在次优方案上。

4. **小步骤提交**：核心原则来自Martin Fowler的《重构》——每个步骤都足够小，让代码库始终处于可工作状态。这降低了重构风险，也便于回滚。

5. **范围明确**：明确写出"范围外"事项，防止范围蔓延。

6. **持久性设计**：
   - 不包含文件路径和代码片段
   - 描述模块、行为、契约而非具体实现
   - 这样Issue在代码库演进后仍有价值

7. **测试考量**：专门评估测试覆盖率并制定测试策略，确保重构有安全网。

### 适用场景

- 大规模重构的规划
- 技术债务清理方案
- 架构演进计划
- 代码迁移策略
- 团队重构任务的分配
