---
name: ubiquitous-language
description: 从当前对话中提取DDD风格的通用语言词汇表，标记歧义并提出规范术语。保存到UBIQUITOUS_LANGUAGE.md。当用户想要定义领域术语、构建词汇表、强化术语、创建通用语言，或提到"domain model"或"DDD"时使用。
---

# 通用语言

从当前对话中提取并形式化领域术语为一致的词汇表，保存到本地文件。

## 流程

1. **扫描对话**寻找领域相关的名词、动词和概念
2. **识别问题**：
   - 同一个词用于不同概念（歧义）
   - 不同的词用于同一个概念（同义词）
   - 模糊或过载的术语
3. **提出规范的词汇表**，包含有主见的术语选择
4. **写入`UBIQUITOUS_LANGUAGE.md`**到工作目录，使用下面的格式
5. **在对话中输出摘要**

## 输出格式

写一个`UBIQUITOUS_LANGUAGE.md`文件，结构如下：

```md
# 通用语言

## 订单生命周期

| 术语        | 定义                                              | 应避免的别名      |
| ----------- | ------------------------------------------------- | ----------------- |
| **订单**    | 客户购买一个或多个商品的请求                      | 购买、交易       |
| **发票**    | 交付后发送给客户的付款请求                        | 账单、付款请求   |

## 人员

| 术语         | 定义                                  | 应避免的别名       |
| ------------ | ------------------------------------- | ------------------ |
| **客户**     | 下订单的个人或组织                    | 客户端、买家、账户 |
| **用户**     | 系统中的认证身份                      | 登录、账户         |

## 关系

- 一张**发票**属于且仅属于一个**客户**
- 一个**订单**产生一张或多张**发票**

## 示例对话

> **开发者：**"当**客户**下**订单**时，我们立即创建**发票**吗？"
> **领域专家：**"不——**发票**只有在**履约**确认后才生成。一个**订单**如果商品分批运输，可能产生多张**发票**。"
> **开发者：**"所以如果一个**出货**在发运前取消，就不会有**发票**？"
> **领域专家：**"正是。**发票**生命周期与**履约**绑定，而非**订单**。"

## 标记的歧义

- "account"被用于同时指代**客户**和**用户**——这是不同的概念：**客户**下订单，而**用户**是认证身份，可能代表也可能不代表**客户**。
```

## 规则

- **要有主见。** 当同一概念存在多个词时，选择最好的一个，将其他列为应避免的别名。
- **明确标记冲突。** 如果术语在对话中被歧义使用，在"标记的歧义"部分指出并给出清晰建议。
- **只包含与领域专家相关的术语。** 跳过模块或类的名称，除非它们在领域语言中有意义。
- **保持定义简洁。** 最多一句话。定义它是什么，而非它做什么。
- **展示关系。** 使用加粗的术语名称并在明显的地方表达基数。
- **只包含领域术语。** 跳过通用编程概念（数组、函数、端点），除非它们有领域特定含义。
- **将术语分组到多个表格**当自然集群出现时（如按子域、生命周期或参与者）。每组有自己的标题和表格。如果所有术语属于单一凝聚的领域，一个表格即可——不要强制分组。
- **编写示例对话。** 开发者和领域专家之间的简短对话（3-5轮），展示术语如何自然交互。对话应该澄清相关概念之间的边界，并展示术语的精确使用。

<示例>

## 示例对话

> **开发者：**"如何在没有Docker的情况下测试**同步服务**？"

> **领域专家：**"提供**文件系统层**而非**Docker层**。它实现了相同的**沙箱服务**接口，但使用本地目录作为**沙箱**。"

> **开发者：**"所以**sync-in**仍然创建**包**并解包？"

> **领域专家：**"正是。**同步服务**不知道它在与哪个层对话。它调用`exec`和`copyIn`——**文件系统层**只是将这些作为本地shell命令运行。"

</示例>

## 重新运行

在同一对话中再次调用时：

1. 读取现有的`UBIQUITOUS_LANGUAGE.md`
2. 整合后续讨论中的任何新术语
3. 如果理解演进，更新定义
4. 重新标记任何新歧义
5. 重写示例对话以整合新术语

---

## 总结与设计思路讲解

### 主要功能

`ubiquitous-language` 是一个**领域语言规范化技能**，帮助团队建立统一、无歧义的领域词汇表。

### 设计思路

1. **DDD核心理念**：来自领域驱动设计（Domain-Driven Design），"通用语言"（Ubiquitous Language）指开发者和领域专家共享的、精确的术语体系。这是代码与业务对齐的基础。

2. **歧义检测**：自动识别对话中的术语问题：
   - 同词异义：一个词用于多个概念
   - 同义异词：多个词表示同一概念
   - 模糊过载：定义不清的术语

3. **有主见的选择**：不是简单记录，而是做出明确建议——选择最佳术语，将其他标记为"应避免的别名"。这避免了词汇表变成"什么都可以"的无效文档。

4. **示例对话**：一个独特且强大的设计——通过展示开发者和领域专家的实际对话来演示术语使用。这比抽象的定义更能传达术语的实际用法和边界。

5. **迭代演进**：支持在同一对话中多次运行，随着讨论深入逐步完善词汇表。

6. **关系建模**：不只是定义术语，还展示它们之间的关系和基数，形成完整的领域模型视图。

### 适用场景

- 新项目启动时的术语定义
- 领域专家与开发团队的沟通桥梁
- 代码命名规范的依据
- 文档术语一致性检查
- 遗留系统理解时的术语梳理
