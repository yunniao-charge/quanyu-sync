---
name: improve-codebase-architecture
description: 探索代码库以发现架构改进机会，专注于通过深化浅模块来提高代码可测试性。当用户想要改进架构、发现重构机会、整合紧密耦合的模块，或使代码库更易于AI导航时使用。
---

# 改进代码库架构

像AI一样探索代码库，发现架构摩擦点，发掘提高可测试性的机会，并以GitHub Issue RFC的形式提出模块深化重构建议。

**深模块**（John Ousterhout，《软件设计哲学》）具有小接口隐藏大实现的特点。深模块更可测试、更易于AI导航，让你可以在边界而非内部进行测试。

## 流程

### 1. 探索代码库

使用Agent工具（subagent_type=Explore）自然地导航代码库。不要遵循僵化的启发式规则——有机地探索并记录你遇到的摩擦：

- 理解一个概念是否需要在许多小文件之间跳转？
- 是否有模块太浅，接口几乎和实现一样复杂？
- 是否有纯函数仅为了可测试性而被提取，但真正的bug隐藏在它们的调用方式中？
- 紧密耦合的模块是否在它们之间的接缝处产生了集成风险？
- 代码库的哪些部分没有测试，或难以测试？

你遇到的摩擦就是信号。

### 2. 展示候选方案

展示一个编号的深化机会列表。对于每个候选方案，显示：

- **集群**：涉及哪些模块/概念
- **耦合原因**：共享类型、调用模式、概念的共同所有权
- **依赖类别**：参见[REFERENCE.md](REFERENCE.md)中的四个类别
- **测试影响**：哪些现有测试会被边界测试替代

不要急于提出接口。询问用户："你想探索哪一个？"

### 3. 用户选择候选方案

### 4. 构建问题空间

在启动子代理之前，为选定的候选方案编写面向用户的问题空间说明：

- 任何新接口需要满足的约束
- 它需要依赖的依赖项
- 一个粗略的示意性代码草图来使约束具体化——这不是提案，只是让约束落地的方式

展示给用户，然后立即进入步骤5。用户阅读和思考问题时，子代理并行工作。

### 5. 设计多个接口

使用Agent工具并行启动3个以上的子代理。每个代理必须为深化的模块产生一个**截然不同**的接口。

为每个子代理提供独立的技术简报（文件路径、耦合详情、依赖类别、隐藏的内容）。此简报独立于步骤4中面向用户的说明。给每个代理不同的设计约束：

- 代理1："最小化接口——最多1-3个入口点"
- 代理2："最大化灵活性——支持多种用例和扩展"
- 代理3："为最常见的调用者优化——让默认情况变得简单"
- 代理4（如适用）："围绕端口与适配器模式设计跨边界依赖"

每个子代理输出：

1. 接口签名（类型、方法、参数）
2. 展示调用者如何使用它的使用示例
3. 它在内部隐藏了什么复杂性
4. 依赖策略（如何处理依赖——参见[REFERENCE.md](REFERENCE.md)）
5. 权衡

按顺序展示设计，然后用文字进行比较。

比较后，给出你自己的建议：你认为哪个设计最强以及原因。如果不同设计的元素可以很好地组合，提出一个混合方案。要有主见——用户想要的是一个明确的判断，而不只是一份菜单。

### 6. 用户选择接口（或接受推荐）

### 7. 创建GitHub Issue

使用`gh issue create`将重构RFC创建为GitHub Issue。使用[REFERENCE.md](REFERENCE.md)中的模板。不要在创建前让用户审阅——直接创建并分享URL。

---

## 总结与设计思路讲解

### 主要功能

`improve-codebase-architecture` 是一个**架构改进发现与规划技能**，帮助识别代码库中的架构问题并生成重构建议。

### 设计思路

1. **摩擦驱动发现**：不使用预设的检查清单，而是通过实际探索代码库时遇到的"摩擦"来发现问题。这些摩擦——理解困难、跳转频繁、测试困难——就是真正需要改进的地方。

2. **深模块理论**：核心概念来自《软件设计哲学》。"深模块"指小接口隐藏大实现的模块，是理想的设计目标；"浅模块"则是接口复杂但实现简单的反模式。

3. **并行设计探索**：使用多个子代理同时探索不同的设计方案，每个代理有不同的设计约束，确保方案之间的根本性差异。

4. **RFC式输出**：最终以GitHub Issue RFC的形式输出，便于团队讨论和追踪，而非直接开始重构。

5. **渐进式确认**：先展示候选问题让用户选择，再深入分析选定的方向，避免在用户不关心的领域浪费时间。

### 适用场景

- 遗留代码重构规划
- 代码可测试性改进
- 架构债务识别与偿还
- 模块边界重新划分
- 团队架构讨论的起点
