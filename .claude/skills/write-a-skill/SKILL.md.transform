---
name: write-a-skill
description: 创建具有正确结构、渐进式披露和捆绑资源的新代理技能。当用户想要创建、编写或构建新技能时使用。
---

# 编写技能

## 流程

1. **收集需求** - 询问用户：
   - 技能覆盖什么任务/领域？
   - 应处理哪些具体用例？
   - 需要可执行脚本还是只需指令？
   - 是否包含任何参考材料？

2. **起草技能** - 创建：
   - 包含简洁指令的SKILL.md
   - 如果内容超过500行，添加额外参考文件
   - 如果需要确定性操作，添加工具脚本

3. **与用户审阅** - 展示草稿并询问：
   - 这是否覆盖了你的用例？
   - 有什么遗漏或不清楚的吗？
   - 任何章节应该更详细还是更简略？

## 技能结构

```
skill-name/
├── SKILL.md           # 主要指令（必需）
├── REFERENCE.md       # 详细文档（如需要）
├── EXAMPLES.md        # 使用示例（如需要）
└── scripts/           # 工具脚本（如需要）
    └── helper.js
```

## SKILL.md模板

```md
---
name: skill-name
description: 能力的简要描述。当[特定触发条件]时使用。
---

# 技能名称

## 快速开始

[最小可行示例]

## 工作流程

[复杂任务的分步流程和检查清单]

## 高级功能

[链接到单独文件：参见[REFERENCE.md](REFERENCE.md)]
```

## 描述要求

描述是**代理在选择加载哪个技能时唯一看到的内容**。它与所有其他已安装技能一起出现在系统提示中。你的代理读取这些描述，根据用户请求选择相关技能。

**目标**：给代理足够的信息以了解：

1. 此技能提供什么能力
2. 何时/为何触发它（特定关键词、上下文、文件类型）

**格式**：

- 最多1024字符
- 使用第三人称编写
- 第一句：它做什么
- 第二句："当[特定触发条件]时使用"

**好例子**：

```
从PDF文件提取文本和表格，填写表单，合并文档。当处理PDF文件或用户提到PDF、表单或文档提取时使用。
```

**坏例子**：

```
帮助处理文档。
```

坏例子让代理无法区分此技能与其他文档技能。

## 何时添加脚本

在以下情况添加工具脚本：

- 操作是确定性的（验证、格式化）
- 相同代码会被反复生成
- 错误需要显式处理

脚本比生成的代码节省token并提高可靠性。

## 何时拆分文件

在以下情况拆分为单独文件：

- SKILL.md超过100行
- 内容有不同领域（财务vs销售模式）
- 高级功能很少需要

## 审查检查清单

起草后，验证：

- [ ] 描述包含触发条件（"当...时使用"）
- [ ] SKILL.md在100行以内
- [ ] 没有时间敏感信息
- [ ] 术语一致
- [ ] 包含具体示例
- [ ] 引用只有一层深度

---

## 总结与设计思路讲解

### 主要功能

`write-a-skill` 是一个**技能创建元技能**——帮助用户创建新的Claude Code技能。

### 设计思路

1. **结构化输出**：定义了清晰的技能包结构：
   - `SKILL.md`：核心指令，必需
   - `REFERENCE.md`：详细参考，可选
   - `EXAMPLES.md`：使用示例，可选
   - `scripts/`：工具脚本，可选

   这提供了标准化的组织方式。

2. **渐进式披露**：主要指令保持在SKILL.md中（<100行），详细内容分离到REFERENCE.md。这避免了一次性加载过多信息。

3. **描述的关键性**：强调description字段是代理选择技能的唯一依据。这要求描述必须：
   - 明确说明能力
   - 包含触发条件（"当...时使用"）

   好的描述让代理能正确判断何时使用该技能。

4. **脚本vs生成代码**：明确了何时应该用脚本而非让AI生成代码：
   - 确定性操作
   - 重复使用的代码
   - 需要显式错误处理

   脚本节省token并提高可靠性。

5. **用户确认循环**：起草后与用户审阅，确保技能真正满足需求。

6. **检查清单**：提供验证清单，确保新技能符合质量标准。

### 适用场景

- 创建自定义Claude Code技能
- 团队知识固化
- 工作流程自动化
- 领域专家知识编码
- 代理能力扩展
