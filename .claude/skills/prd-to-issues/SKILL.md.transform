---
name: prd-to-issues
description: 使用追踪弹垂直切片将PRD拆分为可独立领取的GitHub Issues。当用户想要将PRD转换为Issues、创建实现工单或将PRD拆分为工作项时使用。
---

# PRD转Issues

使用垂直切片（追踪弹）将PRD拆分为可独立领取的GitHub Issues。

## 流程

### 1. 定位PRD

向用户询问PRD的GitHub Issue编号（或URL）。

如果PRD尚未在你的上下文窗口中，使用`gh issue view <编号>`获取它（包括评论）。

### 2. 探索代码库（可选）

如果尚未探索代码库，请先探索以了解代码的当前状态。

### 3. 起草垂直切片

将PRD拆分为**追踪弹**Issues。每个Issue是一个贯穿所有集成层的薄垂直切片，而非某一层的水平切片。

切片可以是'HITL'或'AFK'。HITL切片需要人工交互，如架构决策或设计评审。AFK切片可以在无需人工交互的情况下实现并合并。尽可能优先选择AFK而非HITL。

<垂直切片规则>
- 每个切片交付一条贯穿每一层（模式、API、UI、测试）的窄但完整的路径
- 完成的切片可以独立演示或验证
- 宁可多个薄切片也不要少数厚切片
</垂直切片规则>

### 4. 用户测验

将建议的拆分展示为编号列表。对于每个切片，显示：

- **标题**：简短描述性名称
- **类型**：HITL / AFK
- **被阻塞**：哪些其他切片（如有）必须先完成
- **覆盖的用户故事**：此切片涉及PRD中的哪些用户故事

询问用户：

- 粒度是否合适？（太粗 / 太细）
- 依赖关系是否正确？
- 是否应合并或进一步拆分某些切片？
- HITL和AFK的标记是否正确？

迭代直到用户批准拆分。

### 5. 创建GitHub Issues

对于每个批准的切片，使用`gh issue create`创建GitHub Issue。使用下面的Issue正文模板。

按依赖顺序创建Issues（阻塞者优先），以便在"被阻塞"字段中引用真实的Issue编号。

<Issue模板>
## 父PRD

#<prd-issue-number>

## 构建内容

此垂直切片的简洁描述。描述端到端行为，而非逐层实现。引用父PRD的特定章节而非重复内容。

## 验收标准

- [ ] 标准1
- [ ] 标准2
- [ ] 标准3

## 被阻塞

- 被 #<issue-number> 阻塞（如有）

或"无 - 可立即开始"如果无阻塞。

## 涉及的用户故事

从父PRD按编号引用：

- 用户故事3
- 用户故事7

</Issue模板>

不要关闭或修改父PRD Issue。

---

## 总结与设计思路讲解

### 主要功能

`prd-to-issues` 是一个**需求拆解技能**，将产品需求文档（PRD）转化为可独立开发的GitHub Issues。

### 设计思路

1. **垂直切片原则**：核心设计理念是"追踪弹"（tracer bullet）方法——每个Issue是一条贯穿所有层的完整功能路径，而非某一层的实现。这确保每个Issue都能独立演示和验证。

2. **HITL vs AFK分类**：
   - HITL（Human In The Loop）：需要人工决策的切片
   - AFK（Away From Keyboard）：可完全自动完成的切片

   这种分类帮助团队并行工作——开发者可以领取AFK任务，而HITL任务等待相关决策。

3. **依赖图管理**：通过"被阻塞"字段建立Issue之间的依赖关系，确保开发顺序正确，同时最大化并行度。

4. **粒度迭代**：先展示拆分建议让用户确认粒度，避免过粗或过细的拆分，这在实践中非常实用。

5. **追溯性**：每个Issue都链接到父PRD，保持从需求到任务的完整追溯链。

### 适用场景

- 新功能开发的项目规划
- 大型需求的任务分解
- 敏捷迭代的Story拆分
- 跨团队项目的任务分配
- 产品经理与开发团队的协作桥梁
